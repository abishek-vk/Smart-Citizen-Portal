kind = "web"
previewPath = "/"
title = "Smart City Citizen Portal"
version = "1.0.0"
id = "artifacts/citizen-portal"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24767

[services.development]
run = "pnpm --filter @workspace/citizen-portal run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/citizen-portal", "run", "build" ]
serve = "static"
publicDir = "artifacts/citizen-portal/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24767"
BASE_PATH = "/"
